<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/lex_purple_primary_light"> <!-- Ripple effect color -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" />
            <corners android:radius="24dp" /> <!-- Matches Lexify.Button.Base cornerRadius -->
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/lex_purple_primary" />
            <corners android:radius="24dp" />
            <stroke android:width="1dp" android:color="@color/lex_purple_primary_dark" />
        </shape>
    </item>
</ripple>