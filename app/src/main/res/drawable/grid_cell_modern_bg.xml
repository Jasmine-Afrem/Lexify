<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/game_grid_cell_empty_bg" />
    <corners android:radius="4dp" /> <!-- Subtle rounding -->
    <stroke android:width="2dp" android:color="@color/game_grid_cell_empty_border" />
    <size android:width="50dp" android:height="50dp"/> <!-- Example, adjust in code if needed -->
</shape>